import 'package:flutter/material.dart';

abstract final class AppColors {
  // Brand
  static const primary = Color(0xFF1DA1F2);
  static const primaryDark = Color(0xFF0D8BD9);

  // Neutrals
  static const background = Color(0xFFFFFFFF);
  static const backgroundDark = Color(0xFF15202B);
  static const surface = Color(0xFFF5F8FA);
  static const surfaceDark = Color(0xFF192734);
  static const border = Color(0xFFE1E8ED);
  static const borderDark = Color(0xFF38444D);

  // Text
  static const textPrimary = Color(0xFF0F1419);
  static const textSecondary = Color(0xFF536471);
  static const textPrimaryDark = Color(0xFFE7E9EA);
  static const textSecondaryDark = Color(0xFF8B98A5);

  // Semantic
  static const error = Color(0xFFE0245E);
  static const success = Color(0xFF17BF63);
  static const like = Color(0xFFE0245E);
}
